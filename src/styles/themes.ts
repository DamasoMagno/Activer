import { extendTheme } from '@chakra-ui/react'

export const theme = extendTheme({
  colors: {
    heading: "#7474FE",
    background: "#5636D3",
    primaryText: "#FFF"
  },
  fonts: {
    body: "Scada, sans-serif"
  }
})